<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 渐变 -->
    <gradient
        android:startColor="@color/colorText_blue"
        android:angle="270"
        android:paddingTop="5dp"
        android:endColor="@color/colorText_blue"/>
    <!-- 描边 -->
    <stroke
        android:width="5dp"
        android:color="@color/colorText_blue" />
    <!-- 圆角 -->
    <corners android:radius="10dp" />
</shape>
